extends ../layout.jade
block contenido
  for imagen in imagenes
    div(class='img-container')
      h2= imagen.title
      if(typeof imagen.creator != 'undefined')
        p Created by: #{imagen.creator.email}
      img(src='/public/imagenes/#{imagen._id}.#{imagen.extension}', class='main-img')
